<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chinanurse.cn.nurse">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MinePage.Myinfo" />
        <activity android:name=".Other.MyActivity" />
        <activity android:name=".MinePage.fans_and_attention.Fans" />
        <activity android:name=".MinePage.Mypost" />
        <activity android:name=".WebView.News_WebView"></activity>
        <activity android:name=".MinePage.Mypost"/>
        <activity android:name=".MinePage.Myrecruit"></activity>
    </application>

</manifest>
